# Copy to .env.local for local dev. In CI these come from GitHub Actions secrets
# (see .github/workflows/deploy.yml) — Vite only exposes vars prefixed VITE_.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
